---
const { kicker, title, subtitle } = Astro.props;
---
<div class="section-title">
  {kicker && <div class="kicker">{kicker}</div>}
  <h2 class="h2">{title}</h2>
  {subtitle && <p class="lead" style="margin:0;">{subtitle}</p>}
</div>
